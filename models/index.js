

const Categoria = require('./categoria.db');
const Rol = require('./rol.db');
const Server = require('./server');
const Usuario = require('./usuario.db');
// const Categoria = require('./categoria.db');

module.exports = {
    Categoria,
    Rol,
    Server,
    Usuario
}